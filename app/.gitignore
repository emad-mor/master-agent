node_modules
.next
out
dist
.env
.env.local
*.log
.DS_Store
next-env.d.ts
*.tsbuildinfo

# Local layered memory (core facts + per-project turns/summaries/themes).
# Local-only, never committed.
data/

# Whisper.cpp binaries + model for local speech-to-text. Large, per-machine.
whisper/

# Kokoro TTS — keep the small sidecar SOURCE in git (server.py, requirements.txt,
# start-tts.ps1) so a fresh clone can run voice; ignore the heavy per-machine
# bits (Python venv + ~350 MB neural-voice models + logs).
tts/.venv/
tts/*.onnx
tts/*.bin
tts/*.log
